import React from 'react';
import { Container, Input } from './styles';

const SearchServer: React.FC = () => {
     return (
         <>
            <Container>
                
                <Input placeholder="Encontre uma conversa"/>
            </Container>
            
         </>
     )
}

export default SearchServer;